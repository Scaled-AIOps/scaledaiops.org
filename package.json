{
  "name": "scaledaiops.org",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:local": "./build.sh >/dev/null && (npx playwright install chromium >/dev/null 2>&1 || true) && (python3 -m http.server 8765 -d dist >/dev/null 2>&1 & echo $! > .server.pid) && sleep 1 && FFRS_ENABLED=true npx playwright test --config playwright.local.config.js --reporter=line; s=$?; kill $(cat .server.pid) 2>/dev/null; rm -f .server.pid; exit $s"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Scaled-AIOps/scaledaiops.org.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/Scaled-AIOps/scaledaiops.org/issues"
  },
  "homepage": "https://github.com/Scaled-AIOps/scaledaiops.org#readme",
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
